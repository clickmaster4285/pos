// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "https://3d18afd6c4f1.ngrok-free.app",
  ],
  // other config keys you already have
};

export default nextConfig;
