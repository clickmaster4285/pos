import { HandlePermissions } from '@/components/permissions/handlePermissions';

export default function PermissionsPage() {
  return (
    <main className="min-h-screen bg-background">
      <HandlePermissions />
    </main>
  );
}
